# Mado — 全機能本番稼働 設計書

> 作成日: 2026-04-13
> ステータス: レビュー待ち

---

## 設計思想

この設計書は「機能の有無チェック」ではなく、**2つの視点**で全タスクを評価する。

1. **機能の核** — その機能が存在する本質的な理由。「何ができるか」ではなく「なぜ必要か」
2. **ユーザー視点** — ユーザーが実際に見て、触って、「これでいい」と判断する基準

スコアリングは各タスクに **必須項目（Pass/Fail）** と **品質スコア（0-100）** の2軸を設ける。
必須項目が1つでもFailなら、品質スコアに関わらず不合格。

---

## 全体アーキテクチャ（3つの流れ）

```
【流れ1】申込→サイト完成
  顧客が /start で申込 → Stripe決済 → webhook → サイト自動生成 → 顧客にURL通知

【流れ2】顧客がサイトを編集
  顧客が /member にログイン → 編集依頼を送信 → テキスト/画像は即反映、レイアウト/機能はClaude API経由

【流れ3】Lyoの運営管理
  Lyoが /admin で全顧客を一覧 → 依頼キュー処理 → MRR確認 → サブスク解約対応
```

---

# 流れ1: 申込 → サイト完成

## 現状の根本問題

| 問題 | 影響 | 根本原因 |
|------|------|----------|
| 生成サイトが `ai-art-portfolio` ベース | 顧客のサイトが旧ポートフォリオのまま | `shikumiya-template` リポが未更新 |
| 会社名等が反映されない | サイトがデモデータのまま | テンプレの page.tsx が site.config.json を読んでいない |
| テンプレファイルコピーが不完全 | ビルドエラー or 表示崩れの可能性 | webhook の copyTemplateFiles が旧構造前提 |

## タスク 1-1: テンプレートリポの刷新（最優先）

### 機能の核
> 顧客が決済した瞬間に、**そのテンプレートの正しいサイト**が自動生成される。
> これが壊れている = サービスが成立しない。全ての土台。

### ユーザー視点
> 顧客は決済後1日以内に「自分の会社名・情報が入った本物のサイト」のURLをメールで受け取る。
> デモデータが残っていたり、別テンプレが表示されたら、信頼は即座に崩壊する。

### 作業内容

#### 1-1-A: shikumiya-template リポをクリーンシェル化
- [ ] GitHub上の `AndoLyo/shikumiya-template` を Next.js 16 の最小構成に更新
- [ ] 必要なファイル:
  - `package.json` (next, react, react-dom, tailwindcss, framer-motion, lucide-react)
  - `next.config.ts`
  - `tsconfig.json`
  - `postcss.config.mjs`
  - `tailwind.config.ts`
  - `src/app/layout.tsx` (最小ルートレイアウト)
  - `src/app/page.tsx` (プレースホルダー。webhook がテンプレ用に上書きする)
  - `src/lib/site-config-schema.ts` (型定義)
  - `public/` (空ディレクトリ)
- [ ] 旧 ai-art-portfolio の痕跡を完全除去
- [ ] README に「このリポはMadoの顧客サイトテンプレートです」と明記

#### 1-1-B: テンプレートの page.tsx が site.config.json を読む仕組み
- [ ] 各テンプレート（warm-craft, trust-navy, clean-arch × lite/mid/pro）の page.tsx を改修
- [ ] `public/site.config.json` を fetch or import して全データを注入
- [ ] 具体的な読み込み方式:
  ```typescript
  // src/app/page.tsx の冒頭
  import config from '../../public/site.config.json';
  // または
  import fs from 'fs/promises';
  const config = JSON.parse(await fs.readFile('public/site.config.json', 'utf-8'));
  ```
- [ ] company.name, company.tagline, projects, strengths 等を全セクションで参照
- [ ] プラン別表示制御: testimonials は middle 以上、chatFAQs は premium のみ

#### 1-1-C: ローカルでテンプレ×config の結合テスト
- [ ] 各テンプレ（9種）にサンプル site.config.json を配置してビルド確認
- [ ] 会社名・電話番号・施工事例が正しく表示されるかスクリーンショット確認
- [ ] プラン別機能（testimonials, news, chatbot, booking）の表示/非表示確認

### チェック項目（Pass/Fail）

| # | チェック項目 | 判定基準 | 核/UX |
|---|-------------|---------|-------|
| 1 | テンプレートリポに ai-art-portfolio の痕跡がゼロ | `grep -r "ai-art" .` が空 | 核 |
| 2 | `npm run build` がエラーゼロで通る | exit code 0 | 核 |
| 3 | site.config.json の company.name がページ上に表示される | ブラウザで目視確認 | UX |
| 4 | site.config.json の projects がギャラリーに表示される | 画像+タイトルが一致 | UX |
| 5 | lite テンプレで testimonials セクションが表示されない | DOMに存在しない | 核 |
| 6 | premium テンプレで chatbot UI が表示される | DOM に存在+操作可能 | UX |

### 品質スコア（0-100）

| 観点 | 配点 | 基準 |
|------|------|------|
| データ反映の完全性 | 30 | config の全フィールドが対応する UI に反映される |
| 表示崩れゼロ | 25 | 全9テンプレ×モバイル/デスクトップで崩れなし |
| ビルド速度 | 15 | 30秒以内にビルド完了（Vercel基準） |
| プラン別制御の正確性 | 20 | 各プランで見えるべきものだけ見える |
| コードの保守性 | 10 | 共通コンポーネント化、型安全 |

---

## タスク 1-2: Webhook のテンプレファイルコピー修正

### 機能の核
> webhook は「正しいテンプレートファイルを、正しい構造で、顧客リポにコピーする」パイプライン。
> ファイルが1つ欠けても、import パスが1つ間違っていても、ビルドが通らない = サイトが表示されない。

### ユーザー視点
> 顧客は「決済したのにサイトが見れない」状態を絶対に許容しない。
> エラーが起きても顧客には見えない。だからこそ、裏側の堅牢性が全て。

### 作業内容

#### 1-2-A: copyTemplateFiles の対象ファイルリスト更新
- [ ] 現在の webhook が参照する `portfolio-templates/{templateId}/` のファイルリストを確認
- [ ] 新テンプレ構造に合わせて対象ファイルを更新:
  - `page.tsx` (メインページ)
  - `components/` 配下の全コンポーネント
  - `lib/` 配下のユーティリティ（site-config-schema.ts 等）
- [ ] import パス書き換えロジックの検証（`@/components/portfolio-templates/{id}/` → `@/components/`）
- [ ] DemoBanner 除去ロジックの検証

#### 1-2-B: site.config.json 生成と配置
- [ ] `template-config-generator.ts` の出力が site-config-schema.ts と完全一致するか型チェック
- [ ] 生成された JSON を `public/site.config.json` に push するコードの確認
- [ ] 顧客が入力した companyName, email, phone, tagline, ceo, bio が全て JSON に入るか検証
- [ ] 画像パス（`public/images/`）が JSON 内で正しく参照されるか検証

#### 1-2-C: Vercel デプロイの安定性
- [ ] リポ作成 → ファイル push → Vercel プロジェクト作成 → デプロイの全ステップのエラーハンドリング
- [ ] デプロイ失敗時のリトライロジック（現在なし → 追加）
- [ ] デプロイ完了の検知方法（現在は URL を推測で返している → 実際のデプロイ完了を待つ）
- [ ] GAS への通知に正確な site_url を含める

#### 1-2-D: E2Eテスト（申込→サイト表示）
- [ ] テスト用 Stripe カード（4242...）で /start → 決済
- [ ] webhook が発火し、正しいテンプレのサイトが生成されることを確認
- [ ] 生成サイトの URL にアクセスし、入力した会社名が表示されることを確認
- [ ] GAS のシートに注文データが記録されることを確認
- [ ] 確認メールが送信されることを確認

### チェック項目（Pass/Fail）

| # | チェック項目 | 判定基準 | 核/UX |
|---|-------------|---------|-------|
| 1 | webhook が warm-craft テンプレで正しいファイルをコピーする | 生成リポに page.tsx + 全コンポーネントが存在 | 核 |
| 2 | import パスが全て解決される | `npm run build` がエラーゼロ | 核 |
| 3 | site.config.json が public/ に存在する | GitHub API で確認 | 核 |
| 4 | 顧客の会社名がサイトに表示される | ブラウザで目視 | UX |
| 5 | 画像がサイトに表示される | 壊れた画像アイコンがゼロ | UX |
| 6 | Vercel でサイトが公開状態 | HTTPS でアクセス可能 | 核 |
| 7 | GAS に注文データが記録される | シートの行を確認 | 核 |

### 品質スコア（0-100）

| 観点 | 配点 | 基準 |
|------|------|------|
| ファイルコピーの完全性 | 30 | 全テンプレで漏れゼロ |
| ビルド成功率 | 25 | 9テンプレ全てでビルド成功 |
| デプロイ速度 | 15 | 決済から公開まで5分以内 |
| エラーリカバリ | 15 | 失敗時にリトライ or 管理者通知 |
| ログの追跡可能性 | 15 | 全ステップが /api/logs で追跡可能 |

---

## タスク 1-3: /start フォームの完成度

### 機能の核
> /start は「顧客がサービスに入る唯一の入口」。
> ここの体験が悪ければ、どれだけ良いテンプレートがあっても顧客は離脱する。

### ユーザー視点
> 顧客は「難しいことは何もしたくない」人。
> 業種を選ぶ → テンプレを選ぶ → 名前を入れる → 払う。この4ステップ以上の認知負荷は許容されない。

### 作業内容

#### 1-3-A: 業種選択を industry-registry.ts から読む
- [ ] 現在ハードコードされている業種リスト（construction, builder, architect, other）を registry から動的取得に変更
- [ ] `getActiveIndustries()` を使い、テンプレがある業種だけ表示
- [ ] カテゴリ別グループ表示（建築・不動産 → 工務店、建設会社、設計事務所）
- [ ] 将来の業種追加時にコード変更不要な構造

#### 1-3-B: テンプレプレビューの精度
- [ ] iframe プレビューが実際のテンプレデモページ（/portfolio-templates/*）を表示しているか確認
- [ ] プラン名表示が「おまかせ / まるっとおまかせ / ぜんぶおまかせ」になっているか確認
- [ ] 各プランの違い（含まれる機能）が明確に表示されているか確認

#### 1-3-C: STEP 2 のリアルタイムプレビュー
- [ ] 会社名入力時にデモサイトに即反映される仕組みの動作確認
- [ ] 入力途中でプレビューが壊れないか（空文字、長い名前、特殊文字）

#### 1-3-D: STEP 3 のドメイン・決済
- [ ] ドメイン選択 UI の動作確認（既存ドメイン / 新規 / サブドメイン）
- [ ] Stripe 決済セッション作成が全プランで成功するか確認
- [ ] 決済後の /start/success ページに遷移するか確認

### チェック項目（Pass/Fail）

| # | チェック項目 | 判定基準 | 核/UX |
|---|-------------|---------|-------|
| 1 | 業種が registry から動的に読まれる | ハードコード値がゼロ | 核 |
| 2 | テンプレプレビューが正しいデモページを表示 | URL が /portfolio-templates/{id} | UX |
| 3 | 全3プランで Stripe 決済セッションが作成される | Stripe ダッシュボードで確認 | 核 |
| 4 | 決済後に /start/success に遷移する | URL 遷移を確認 | UX |
| 5 | バリデーションが全フィールドで動作する | 空送信でエラーメッセージ表示 | UX |

### 品質スコア（0-100）

| 観点 | 配点 | 基準 |
|------|------|------|
| 操作ステップの少なさ | 30 | 4ステップ以内で完了 |
| 認知負荷の低さ | 25 | 説明文を読まなくても次に何をすればいいかわかる |
| プレビューの正確性 | 20 | 実際のサイトと乖離がない |
| エラー時のガイダンス | 15 | 何が間違っているか明確に伝える |
| レスポンシブ対応 | 10 | スマホでも申込完了できる |

---

## タスク 1-4: 決済完了後の通知フロー

### 機能の核
> 決済後の通知は「顧客の不安を消す」ためにある。
> お金を払った直後に何も来なければ、顧客は「騙されたのでは」と不安になる。

### ユーザー視点
> 顧客が期待するのは: (1) 決済直後の確認メール、(2) サイト完成時のURL付きメール。
> この2通が来れば安心する。来なければ問い合わせが発生する。

### 作業内容

#### 1-4-A: GAS メール送信の検証
- [ ] 決済直後の確認メール（注文ID、プラン名、会社名を含む）が送信されるか
- [ ] サイト完成時のURL付きメールが送信されるか
- [ ] メール文面が新プラン名（おまかせ/まるっと/ぜんぶ）になっているか
- [ ] メールが迷惑メールに入らないか（SPF/DKIM は GAS 管轄外だが確認）

#### 1-4-B: /start/success ページの情報充実
- [ ] 注文ID の表示（問い合わせ時に必要）
- [ ] 「サイト完成まで1日以内」の明示
- [ ] /member へのログイン案内（orderId + email でログイン）

### チェック項目（Pass/Fail）

| # | チェック項目 | 判定基準 | 核/UX |
|---|-------------|---------|-------|
| 1 | 決済直後に確認メールが届く | メール受信確認 | 核 |
| 2 | サイト完成時にURL付きメールが届く | メール受信 + URL アクセス可能 | 核 |
| 3 | /start/success に注文IDが表示される | 画面に表示 | UX |
| 4 | メール文面のプラン名が正しい | 「おまかせ」等の表記 | UX |

### 品質スコア（0-100）

| 観点 | 配点 | 基準 |
|------|------|------|
| メール到達率 | 40 | テスト5回中5回届く |
| 情報の過不足 | 30 | 必要な情報が全て、不要な情報がゼロ |
| 安心感の提供 | 30 | 「次に何が起きるか」が明確 |

---

# 流れ2: 顧客がサイトを編集する

## 現状の根本問題

| 問題 | 影響 | 根本原因 |
|------|------|----------|
| /member が全てデモデータ | 顧客は自分のサイト情報を見れない | API未接続。ハードコードの「山田工務店」 |
| 編集依頼フォーム → API 未接続 | 編集依頼を送っても何も起きない | フロントエンドがAPIを呼んでいない |
| Claude API 未実装 | レイアウト/機能変更ができない | /api/edit-request が受けるだけで処理しない |
| プラン変更 UI → API 未接続 | プランを変えられない | settings ページが API を呼んでいない |

---

## タスク 2-1: /member ダッシュボードの実データ接続

### 機能の核
> /member は「顧客が自分のサイトを管理する場所」。
> ここにデモデータが表示されている = 顧客にとってサービスが存在しないのと同じ。

### ユーザー視点
> 顧客がログインして最初に見るのは「自分の会社名」「自分のサイトURL」「自分のプラン」。
> これが「山田工務店」だったら、即座に「壊れてる」と判断する。

### 作業内容

#### 2-1-A: ログイン → プロフィール取得の接続
- [ ] /member ログイン → `/api/member/auth` → GAS verify → 認証成功
- [ ] 認証成功後、`/api/member/[orderId]?email=xxx` でプロフィール取得
- [ ] 取得データ: orderId, companyName, email, phone, plan, template, siteUrl, domain, editsUsed, editsMax, status, createdAt
- [ ] MemberCtx にリアルデータを注入（現在のハードコード「サンプル会社」を置換）

#### 2-1-B: ダッシュボード表示の実データ化
- [ ] 会社名: GAS から取得した companyName
- [ ] ドメイン: GAS から取得した domain or siteUrl
- [ ] ステータス: GAS の status フィールド（制作中/公開中/停止）
- [ ] 編集回数: editsUsed / editsMax をリアルタイム表示
- [ ] 最終更新日: GAS から取得
- [ ] 最近の依頼リスト: `/api/member/edit` の履歴を GAS から取得

#### 2-1-C: GAS 側の API 拡充
- [ ] `action=get_member_profile` — orderId + email で全プロフィール返却
- [ ] `action=get_edit_history` — orderId の編集依頼履歴を返却
- [ ] 既存の `action=verify` を拡張するか、新アクションを追加するか判断

### チェック項目（Pass/Fail）

| # | チェック項目 | 判定基準 | 核/UX |
|---|-------------|---------|-------|
| 1 | ログイン後に自分の会社名が表示される | 「山田工務店」がゼロ箇所 | UX |
| 2 | 自分のサイト URL が表示され、クリックで開ける | リンクが正しい | UX |
| 3 | 編集回数が正確（editsUsed / editsMax） | GAS のデータと一致 | 核 |
| 4 | プランに応じた機能制御が正しい | lite で premium 機能が見えない | 核 |
| 5 | 存在しない orderId でログインするとエラー | 適切なメッセージ表示 | UX |
| 6 | ハードコードのデモデータが本番コードに残っていない | grep で確認 | 核 |

### 品質スコア（0-100）

| 観点 | 配点 | 基準 |
|------|------|------|
| データの正確性 | 35 | 全フィールドが GAS と一致 |
| 表示速度 | 20 | ログイン → 表示まで 3 秒以内 |
| エラーハンドリング | 20 | GAS 障害時にも「読み込み中」等の表示 |
| 情報の読みやすさ | 25 | 顧客が「今の状態」を一目で理解できる |

---

## タスク 2-2: テキスト/画像の即時編集

### 機能の核
> 「写真を送るだけ。あとは全部おまかせ」が事業の核。
> 顧客が「このテキスト変えたい」「この写真変えたい」と思った瞬間に、選ぶだけで完了する。

### ユーザー視点
> 顧客は PC が苦手。「ここを変えたい」を選択肢から選ぶだけ。
> 変更がいつ反映されるかが見えること（「反映完了しました」の通知）。

### 作業内容

#### 2-2-A: edit-request ページの実データ化
- [ ] 現在の `CURRENT_SITE` ハードコードを、`/api/site-content` から取得に変更
- [ ] site.config.json の内容をセクション別に表示（hero, works, about, contact）
- [ ] 各セクションの現在値をフォームの初期値に設定

#### 2-2-B: テキスト変更 → /api/site-update 接続
- [ ] ユーザーがテキストフィールドを編集 → 送信 → `/api/site-update` を呼出
- [ ] change type: "text", configPath: "company.tagline" 等のドット記法で指定
- [ ] 成功時: 「変更が反映されました」メッセージ + editsUsed のカウントアップ
- [ ] 失敗時: 具体的なエラーメッセージ（「月の編集回数を超えました」等）

#### 2-2-C: 画像変更 → /api/site-update 接続
- [ ] 画像選択 → クロップ UI → `/api/upload-images` でアップロード
- [ ] アップロード完了後 → `/api/site-update` で画像パス更新
- [ ] change type: "image", path: "public/images/hero.jpg"
- [ ] プレビュー表示（変更前 → 変更後の比較）

#### 2-2-D: 反映確認の仕組み
- [ ] サイト更新後に Vercel の自動リデプロイが走る（Git push がトリガー）
- [ ] 「反映には数分かかります」のメッセージ表示
- [ ] 理想: デプロイ完了後に「反映完了」通知（Vercel Deploy Hook or ポーリング）

### チェック項目（Pass/Fail）

| # | チェック項目 | 判定基準 | 核/UX |
|---|-------------|---------|-------|
| 1 | テキスト変更が site.config.json に反映される | GitHub で JSON 差分確認 | 核 |
| 2 | 画像変更がリポの public/images/ に反映される | GitHub でファイル確認 | 核 |
| 3 | 変更後にサイトが自動リデプロイされる | サイトを再読込で確認 | 核 |
| 4 | 編集回数がカウントされる | editsUsed が +1 | 核 |
| 5 | 月の上限を超えるとエラーが出る | エラーメッセージ表示 | UX |
| 6 | 編集フォームに現在値がプリセットされている | ハードコード値ゼロ | UX |

### 品質スコア（0-100）

| 観点 | 配点 | 基準 |
|------|------|------|
| 操作の簡単さ | 30 | 「選んで送る」だけで完了 |
| 反映速度 | 25 | 変更送信 → サイト反映まで 5 分以内 |
| 現在値の正確な表示 | 20 | 編集フォームが今のサイトと完全一致 |
| エラー時のガイダンス | 15 | 何が間違っているか明確 |
| 変更履歴の可視性 | 10 | いつ何を変えたかがわかる |

---

## タスク 2-3: プラン変更機能

### 機能の核
> プラン変更は「顧客がサービスに満足して、もっと使いたい」を受け止める仕組み。
> アップグレードが簡単 = 収益増。ダウングレードが透明 = 信頼維持。

### ユーザー視点
> 顧客は「今のプランで何ができて、上のプランで何が増えるか」を比較したい。
> 変更したら「いつから変わるか」「いくらになるか」を知りたい。

### 作業内容

#### 2-3-A: settings ページの実データ化
- [ ] 現在のハードコード情報を GAS API から取得に変更
- [ ] 現在のプラン、月額、次回請求日を表示
- [ ] 各プランの機能比較を industry-registry.ts のデータから動的生成

#### 2-3-B: プラン変更 → /api/plan-change 接続
- [ ] アップグレード: 即時反映（日割り計算）
- [ ] ダウングレード: 次の請求日から反映
- [ ] 変更前の確認ダイアログ（「¥3,000 → ¥8,000 に変更します。よろしいですか？」）
- [ ] Stripe のサブスクリプション更新が成功するか確認

#### 2-3-C: プラン変更後のサイト更新
- [ ] plan 変更 → site.config.json の plan フィールド更新
- [ ] 新プランの機能が自動的にサイトに追加される（テンプレートの条件分岐）
- [ ] 例: lite → middle にすると testimonials セクションが出現

### チェック項目（Pass/Fail）

| # | チェック項目 | 判定基準 | 核/UX |
|---|-------------|---------|-------|
| 1 | Stripe サブスクが正しく更新される | Stripe ダッシュボードで確認 | 核 |
| 2 | 変更後に /member の表示プランが更新される | 画面で確認 | UX |
| 3 | アップグレード後に新機能がサイトに出現する | サイトで確認 | 核 |
| 4 | ダウングレード時に確認ダイアログが出る | UI で確認 | UX |
| 5 | GAS のプラン情報が更新される | シート確認 | 核 |

### 品質スコア（0-100）

| 観点 | 配点 | 基準 |
|------|------|------|
| 料金の透明性 | 35 | 変更前に金額差が明示される |
| プラン比較のわかりやすさ | 25 | 「今のプラン」と「変更先」の差が一目でわかる |
| 反映のシームレスさ | 25 | 操作 → 反映の間に手動作業ゼロ |
| エラー時の安全性 | 15 | Stripe 失敗時に元のプランが維持される |

---

## タスク 2-4: レイアウト/機能変更（Claude API 連携）

### 機能の核
> テキスト/画像は機械的に差し替えできる。しかし「セクションの順番を変えたい」「新しいセクションを追加したい」は
> コードの変更が必要。Claude API がこれを自動化することで、顧客は技術を意識せずにサイトを進化させられる。

### ユーザー視点
> 顧客は「ここにお客様の声を追加したい」とだけ言えばいい。
> コードが変わること自体は見えなくていい。結果として「サイトに追加された」ことが見えればいい。

### 作業内容

#### 2-4-A: /api/edit-request に Claude API 実装
- [ ] 現在の page.tsx を顧客リポから fetch
- [ ] Claude API に送信するシステムプロンプト設計:
  - site.config.json のスキーマ定義
  - テンプレートのコンポーネント構造
  - 変更依頼の内容
  - 出力: 修正後の page.tsx
- [ ] Claude API のレスポンスを受けて feature ブランチに push
- [ ] Vercel のプレビューデプロイを生成

#### 2-4-B: スコアリングシステム設計

**スコアリングの核**: Claude が生成したコードが「壊れていないか」「意図通りか」を自動判定する。
人間のレビュー負荷をゼロに近づけることが目的。

```
スコアリング 5軸（各20点、合計100点）:

1. ビルド成功（20点）
   - feature ブランチで `npm run build` が通るか
   - 0点: ビルドエラー → 即却下
   - 20点: エラーゼロ

2. 型安全性（20点）
   - TypeScript の型エラーがないか
   - site-config-schema.ts の型と整合するか
   - 0点: 型エラーあり
   - 20点: 型エラーゼロ

3. 意図一致（20点）
   - 変更依頼の内容と実際の差分が一致するか
   - Claude API のセルフチェック（変更内容を要約させて依頼と比較）
   - 0-20点: 一致度に応じて

4. 既存機能の保全（20点）
   - 変更前に存在したセクションが消えていないか
   - DOM の構造比較（変更前後のセクション数）
   - 0点: セクションが消えた
   - 20点: 全セクション保全

5. コード品質（20点）
   - import の整合性
   - 不要なコメントや TODO が残っていないか
   - インデントの一貫性
   - 0-20点: 品質に応じて
```

**自動承認ルール:**
- 80点以上 → 自動で main にマージ、Vercel 自動デプロイ
- 60-79点 → Lyo にレビュー依頼（/admin/requests に表示）
- 59点以下 → 自動却下、Claude に再生成を依頼（最大2回リトライ）

#### 2-4-C: 管理者レビュー UI
- [ ] /admin/requests にスコア付きで依頼を表示
- [ ] プレビュー URL（Vercel の feature ブランチデプロイ）へのリンク
- [ ] 「承認」→ main マージ → 本番デプロイ
- [ ] 「差し戻し」→ 修正指示を入力 → Claude API に再送

### チェック項目（Pass/Fail）

| # | チェック項目 | 判定基準 | 核/UX |
|---|-------------|---------|-------|
| 1 | Claude API が page.tsx を受け取り、修正版を返す | API レスポンスに修正コードが含まれる | 核 |
| 2 | feature ブランチが作成される | GitHub で確認 | 核 |
| 3 | スコアリングが5軸全てで実行される | ログに5軸のスコアが記録 | 核 |
| 4 | 80点以上で自動マージされる | main ブランチに変更が反映 | 核 |
| 5 | 59点以下で自動却下される | 依頼ステータスが「却下」 | 核 |
| 6 | /admin/requests にスコアが表示される | UI で確認 | UX |
| 7 | プレビュー URL が機能する | ブラウザでアクセス可能 | UX |

### 品質スコア（0-100）

| 観点 | 配点 | 基準 |
|------|------|------|
| 変更の正確性 | 30 | 依頼内容と結果の乖離がない |
| 自動承認の信頼性 | 25 | 自動承認したものが壊れていない（偽陽性ゼロ） |
| レビュー負荷 | 20 | 80%以上の依頼が自動承認で処理される |
| 処理速度 | 15 | 依頼 → 反映まで 10 分以内 |
| リカバリ | 10 | 失敗時にロールバック可能 |

---

# 流れ3: Lyoの運営管理

## 現状の根本問題

| 問題 | 影響 | 根本原因 |
|------|------|----------|
| /admin の顧客一覧がデモデータ混在 | 実際の顧客数がわからない | GAS に全顧客取得のアクションがない |
| /admin/requests が完全デモ | 編集依頼の処理ができない | GAS に依頼取得アクションがない |
| MRR が「—」表示 | 収益が把握できない | Stripe API 未接続 |
| サブスク解約の処理がない | 解約されても気づかない | webhook の subscription.deleted が未実装 |

---

## タスク 3-1: /admin ダッシュボードの実データ化

### 機能の核
> /admin は Lyo が「事業の今」を一目で把握する場所。
> 顧客数、MRR、pending の依頼。この3つが見えれば事業判断ができる。

### ユーザー視点（Lyo視点）
> 朝一番に /admin を開いて「今日やるべきこと」がわかる状態。
> 数字は正確で、アクションが必要なものはハイライトされている。

### 作業内容

#### 3-1-A: GAS 全顧客取得 API
- [ ] `action=get_all_customers` を GAS に追加
- [ ] 返却データ: orderId, companyName, email, plan, template, status, siteUrl, createdAt, stripeSubscriptionId
- [ ] ステータスフィルタ: all / active / cancelled / pending

#### 3-1-B: /admin ダッシュボード KPI
- [ ] 顧客数: GAS の全顧客カウント（ステータス別）
- [ ] MRR: 全アクティブ顧客のプラン月額の合計（GAS データから計算 or Stripe API）
- [ ] pending 依頼数: GAS の未処理依頼カウント
- [ ] 月間 PV: 将来実装（今は「—」でOK、ただし「未実装」と明記）

#### 3-1-C: /admin/accounts の実データ化
- [ ] ハードコードのデモデータを GAS API に置換
- [ ] 検索機能: 会社名、メール、orderId で検索
- [ ] ステータスバッジ: 公開中（緑）/ 制作中（黄）/ 停止（赤）
- [ ] 各顧客行のクリックで詳細（サイトURL、編集履歴、Stripe状態）

#### 3-1-D: /admin/requests の実データ化
- [ ] ハードコードの `INITIAL_REQUESTS` を GAS API に置換
- [ ] `action=get_edit_requests` を GAS に追加
- [ ] ステータスフィルタ: pending / in_progress / completed / rejected
- [ ] 各依頼にスコア表示（Claude API スコアリング結果）
- [ ] 「承認」「差し戻し」アクションの実装

### チェック項目（Pass/Fail）

| # | チェック項目 | 判定基準 | 核/UX |
|---|-------------|---------|-------|
| 1 | /admin に実際の顧客数が表示される | GAS のデータと一致 | 核 |
| 2 | MRR が計算されて表示される | アクティブ顧客×月額の合計 | 核 |
| 3 | /admin/accounts に全顧客が表示される | デモデータがゼロ | 核 |
| 4 | /admin/requests に実際の依頼が表示される | GAS の依頼データと一致 | 核 |
| 5 | 検索機能が動作する | 会社名で絞り込める | UX |
| 6 | ステータスバッジが正しい色で表示される | 公開中=緑、制作中=黄、停止=赤 | UX |

### 品質スコア（0-100）

| 観点 | 配点 | 基準 |
|------|------|------|
| データの正確性 | 35 | 全 KPI が実データに基づく |
| 情報の即時性 | 20 | ページ読込で最新データが表示 |
| アクションの明確さ | 20 | 何をすべきかが一目でわかる |
| 操作効率 | 15 | 3クリック以内で目的の情報に到達 |
| レスポンシブ | 10 | タブレットでも管理可能 |

---

## タスク 3-2: サブスクリプションライフサイクル

### 機能の核
> サブスクビジネスの生命線は「解約を検知し、対応し、記録する」こと。
> 解約が検知されなければ、サイトが動き続けてコストだけ発生する。

### ユーザー視点（顧客 + Lyo）
> 顧客: 解約したらサイトが止まることを事前に知っている。猶予期間がある。
> Lyo: 解約を即座に検知し、サイト停止 or 猶予期間の管理ができる。

### 作業内容

#### 3-2-A: webhook の subscription イベント処理
- [ ] `customer.subscription.updated`: プラン変更を GAS に記録 + site.config.json 更新
- [ ] `customer.subscription.deleted`: 解約を GAS に記録 + ステータスを「停止」に変更
- [ ] `invoice.payment_failed`: 支払い失敗を GAS に記録 + Lyo に通知

#### 3-2-B: 解約時のサイト処理
- [ ] 即座にサイトを消すのではなく、猶予期間（30日）を設ける
- [ ] 猶予期間中: サイトは表示されるが「このサイトは停止予定です」のバナーを表示（オプション）
- [ ] 猶予期間後: Vercel プロジェクトを無効化（削除ではない）
- [ ] 復帰時: プロジェクトを再有効化するだけで復旧可能

#### 3-2-C: GAS のステータス管理
- [ ] ステータス遷移: 制作中 → 公開中 → 停止予定 → 停止
- [ ] 各遷移時にメール通知（顧客 + Lyo）
- [ ] /admin でステータス別フィルタ

### チェック項目（Pass/Fail）

| # | チェック項目 | 判定基準 | 核/UX |
|---|-------------|---------|-------|
| 1 | Stripe 解約イベントが webhook で処理される | GAS にステータス変更が記録 | 核 |
| 2 | 支払い失敗イベントが処理される | Lyo に通知が届く | 核 |
| 3 | 解約後にサイトが即座に消えない | 30日間はアクセス可能 | UX |
| 4 | /admin でステータスが「停止予定」に変わる | 画面で確認 | UX |

### 品質スコア（0-100）

| 観点 | 配点 | 基準 |
|------|------|------|
| イベント処理の確実性 | 35 | 全 Stripe イベントが漏れなく処理される |
| 顧客への透明性 | 25 | 解約プロセスが顧客に明確に伝わる |
| データの整合性 | 25 | GAS + Stripe + /admin の情報が一致 |
| 復帰の容易さ | 15 | 復帰時にデータ損失なし |

---

# 実行順序（依存関係に基づく）

```
Phase 1: 土台（流れ1の前提）
├── 1-1: テンプレートリポ刷新        ← 全ての土台。これなしに何も動かない
└── 1-2: Webhook 修正               ← 1-1 の完了が前提

Phase 2: 申込フロー完成（流れ1）
├── 1-3: /start フォーム完成         ← 1-1, 1-2 と並行可能
├── 1-4: 通知フロー                  ← 1-2 の完了が前提
└── E2Eテスト                        ← 全タスク完了後

Phase 3: 顧客管理の実データ化（流れ2の前提）
├── 3-1-A: GAS 全顧客取得 API        ← 独立して着手可能
├── 2-1: /member 実データ接続        ← 3-1-A の完了が前提
└── 3-1: /admin 実データ化           ← 3-1-A の完了が前提

Phase 4: 編集機能（流れ2）
├── 2-2: テキスト/画像の即時編集     ← 2-1 の完了が前提
├── 2-3: プラン変更                  ← 2-1 の完了が前提
└── 2-4: Claude API 連携            ← 2-2 の完了が前提（段階的に）

Phase 5: 運営の安定化（流れ3）
├── 3-2: サブスクライフサイクル      ← 3-1 の完了が前提
└── 全体E2Eテスト                    ← 全 Phase 完了後
```

---

# 全タスク一覧（チェックリスト）

| Phase | タスク | 依存 | 推定規模 |
|:-----:|--------|------|:--------:|
| 1 | 1-1: テンプレートリポ刷新 | なし | 大 |
| 1 | 1-2: Webhook 修正 | 1-1 | 大 |
| 2 | 1-3: /start フォーム完成 | なし | 中 |
| 2 | 1-4: 通知フロー | 1-2 | 小 |
| 3 | 3-1-A: GAS 全顧客取得 API | なし | 中 |
| 3 | 2-1: /member 実データ接続 | 3-1-A | 大 |
| 3 | 3-1: /admin 実データ化 | 3-1-A | 大 |
| 4 | 2-2: テキスト/画像の即時編集 | 2-1 | 大 |
| 4 | 2-3: プラン変更 | 2-1 | 中 |
| 4 | 2-4: Claude API 連携 | 2-2 | 特大 |
| 5 | 3-2: サブスクライフサイクル | 3-1 | 中 |
| 5 | 全体 E2E テスト | 全て | 大 |

---

# 補足: 既存コードの整理

### 削除候補
- `/api/checkout/route.ts` — 旧 API。/api/start に完全置換済み
- `src/lib/site-data.ts` — 旧アーティストポートフォリオ用。SaaS テンプレでは不使用
- `src/lib/template-forms.ts` — 旧アーティストテンプレ用フォーム定義。不使用
- `src/lib/SiteDataContext.tsx` — 旧コンテキスト。member-context.ts に置換済み

### 注意
- `template-site/` ディレクトリ内の6テンプレ（ai-art-portfolio 等）は旧ポートフォリオ用。SaaS の建築テンプレ（warm-craft 等）とは完全に別物
- 旧テンプレ10種（/portfolio-templates/）はデモ表示用として残すが、顧客サイト生成には使わない
